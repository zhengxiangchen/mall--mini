  <i-cell-group>
    <view wx:for="{{orders}}" wx:key="index">
      <i-cell title="下单时间:{{item.createTime}}" label="订单号:{{item.uuid}}" 
      is-link url="/pages/business/orderDetails/orderDetails?uuid={{item.uuid}}&orderStatus={{item.orderStatus}}" 
      value="{{item.orderStatus}}"></i-cell>
    </view>
  </i-cell-group>
